diff --git a/node_modules/expo-audio/.DS_Store b/node_modules/expo-audio/.DS_Store
new file mode 100644
index 0000000..3a2f51a
Binary files /dev/null and b/node_modules/expo-audio/.DS_Store differ
diff --git a/node_modules/expo-audio/build/ExpoAudio.js b/node_modules/expo-audio/build/ExpoAudio.js
index e10e341..e507dc5 100644
--- a/node_modules/expo-audio/build/ExpoAudio.js
+++ b/node_modules/expo-audio/build/ExpoAudio.js
@@ -79,7 +79,7 @@ export function useAudioPlayer(source = null, options = {}) {
     const initialSource = useMemo(() => {
         return downloadFirst ? null : resolveSource(source);
     }, [JSON.stringify(source), downloadFirst]);
-    const player = useReleasingSharedObject(() => new AudioModule.AudioPlayer(initialSource, updateInterval, keepAudioSessionActive), [JSON.stringify(initialSource), updateInterval, keepAudioSessionActive]);
+    const player = useReleasingSharedObject(() => new AudioModule.AudioPlayer(initialSource, updateInterval, keepAudioSessionActive, 0), [JSON.stringify(initialSource), updateInterval, keepAudioSessionActive]);
     // Handle async source resolution for downloadFirst
     useEffect(() => {
         if (!downloadFirst || source === null) {
@@ -298,7 +298,7 @@ export function useAudioRecorderState(recorder, interval = 500) {
 export function createAudioPlayer(source = null, options = {}) {
     const { updateInterval = 500, downloadFirst = false, keepAudioSessionActive = false } = options;
     const initialSource = downloadFirst ? null : resolveSource(source);
-    const player = new AudioModule.AudioPlayer(initialSource, updateInterval, keepAudioSessionActive);
+    const player = new AudioModule.AudioPlayer(initialSource, updateInterval, keepAudioSessionActive, 0);
     if (downloadFirst && source) {
         resolveSourceWithDownload(source)
             .then((resolved) => {
diff --git a/node_modules/expo-audio/ios/MediaController.swift b/node_modules/expo-audio/ios/MediaController.swift
index 2dd3fb2..1d8996b 100644
--- a/node_modules/expo-audio/ios/MediaController.swift
+++ b/node_modules/expo-audio/ios/MediaController.swift
@@ -37,8 +37,9 @@ class MediaController {
     }
     var nowPlayingInfo = nowPlayingInfoCenter.nowPlayingInfo ?? [String: Any]()
 
-    nowPlayingInfo[MPMediaItemPropertyPlaybackDuration] = player.duration
-    nowPlayingInfo[MPNowPlayingInfoPropertyElapsedPlaybackTime] = player.currentTime
+    nowPlayingInfo[MPNowPlayingInfoPropertyIsLiveStream] = true
+    nowPlayingInfo.removeValue(forKey: MPMediaItemPropertyPlaybackDuration)
+    nowPlayingInfo.removeValue(forKey: MPNowPlayingInfoPropertyElapsedPlaybackTime)
     nowPlayingInfo[MPNowPlayingInfoPropertyPlaybackRate] = player.isPlaying ? player.ref.rate : 1.0
     nowPlayingInfo[MPNowPlayingInfoPropertyMediaType] = MPNowPlayingInfoMediaType.audio.rawValue
 
@@ -204,7 +205,7 @@ class MediaController {
     remoteCommandCenter.playCommand.isEnabled = true
     remoteCommandCenter.pauseCommand.isEnabled = true
     remoteCommandCenter.togglePlayPauseCommand.isEnabled = true
-    remoteCommandCenter.changePlaybackPositionCommand.isEnabled = true
+    remoteCommandCenter.changePlaybackPositionCommand.isEnabled = false
     remoteCommandCenter.skipForwardCommand.isEnabled = options?.showSeekForward ?? false
     remoteCommandCenter.skipBackwardCommand.isEnabled = options?.showSeekBackward ?? false
   }
